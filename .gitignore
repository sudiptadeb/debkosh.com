# Nuxt
.nuxt
.data
dist

# Build output: ignore everything except the generated static site,
# which we commit for quick local viewing (CI keeps it up to date).
.output/*
!.output/public

# Dependencies
node_modules

# Logs
logs
*.log
npm-debug.log*

# Env / local
.env
.env.*
!.env.example
.DS_Store

# Cache
.cache
